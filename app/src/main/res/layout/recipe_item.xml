<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/recipe_item_height"
    android:layout_margin="@dimen/default_margin"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:padding="@dimen/default_padding">

            <TextView
                android:id="@+id/recipe_item_title_text_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:textSize="@dimen/recipe_item_title_text_size"
                android:textStyle="bold"
                android:maxLines="@integer/recipe_item_title_text_view_max_size"
                android:ellipsize="end"/>

            <androidx.appcompat.widget.AppCompatImageButton
                android:id="@+id/recipe_item_like_button"
                android:layout_width="@dimen/recipe_item_like_button_size"
                android:layout_height="@dimen/recipe_item_like_button_size"
                android:background="?attr/selectableItemBackground"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                android:src="@drawable/ic_favorite_border" />
        </androidx.constraintlayout.widget.ConstraintLayout>

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/recipe_item_image_view"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:scaleType="centerCrop" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>